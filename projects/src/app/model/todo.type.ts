export type Todo = {
    userId: number;
    completed: boolean;
    title: string;
    id: number;
}

//this is a type definition for a Todo object. 
// it is a custom type that describes the structure of a Todo object
// kind of similar to a class in other languages, however it doesn't have methods,
// it is solely for formatting data.